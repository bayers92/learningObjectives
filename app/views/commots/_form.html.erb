<%= form_for(@commot) do |f| %>
  <% if @commot.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@commot.errors.count, "error") %> prohibited this commot from being saved:</h2>

      <ul>
      <% @commot.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :post_id %><br>
    <%= f.number_field :post_id %>
  </div>
  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
